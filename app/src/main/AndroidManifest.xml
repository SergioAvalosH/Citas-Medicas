<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.recyclerview.app_citas_medicas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appcitasmedicas">
        <activity android:name=".Pacientes">

        </activity>
        <activity android:name=".Prueba" />
        <activity android:name=".view.ui.activity.Boton" /> <!-- Para agregar google maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" /> <!-- Aqui la lista de  Activities -->
        <activity android:name=".view.ui.activity.PacienteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin_menu" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".view.ui.activity.MainActivity">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>